services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_PATH=/app/data/meal_history_clean.csv
      - MEAL_HISTORY_ENABLE_GRU=1
      - MEAL_HISTORY_ENABLE_ADVANCED=1