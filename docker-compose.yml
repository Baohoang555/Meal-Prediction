services:
  meal-history:
    build: .
    ports:
      - "8001:8000"
    environment:
      MEAL_HISTORY_DATA: /app/data/MealHistory_UTF8.csv
    volumes:
      - ./MealHistory_UTF8.csv:/app/data/MealHistory_UTF8.csv:ro
